window.addEventListener('DOMContentLoaded', function () {
  // variabel-start
  // variabel-end
  // Navigation-start
  function navigation(cl,nm) {
    nav_icon = document.getElementsByClassName(cl);
    nav_manue = document.getElementsByClassName(nm);
  }
  navigation('nav-icon','nav-manue');
  // Navigation-end









});
